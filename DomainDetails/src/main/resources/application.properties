spring.application.name=DomainDetails
spring.config.import=optional:configserver:http://localhost:8888
management.endpoints.web.exposure.include=*

#All the below propreties reading from the conifg server
#spring.datasource.url=jdbc:mysql://localhost:3306/Organization?useSSL=false
#spring.datasource.username=root
#spring.datasource.password=mysql@123

#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
#spring.jpa.show-sql: true
#spring.jpa.properties.hibernate.format_sql=true
#spring.jpa.properties.hibernate.enable_lazy_load_no_trans=true 
#eureka.client.serviceUrl.defaultZone=http://localhost:8761/eureka/
#server.port:8082

#profiles
spring.profiles.active=qa
#spring.profiles.include=cource
#above property-- cources is example( here we can give any profile name like @profile("cource") annotation having any java class that will execute it)
#when interface implements the two clasess and calling of interface will have ambiguity to resolve @primary or @qualifier we are using in same way, we can give @profile("name") annotation it dominates the primary annotation
